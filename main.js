let suma = 0;
let x;
let list =[];

for (x=1; x<=10; x++){
    suma += x;
    list.push(x);
}

alert(`La suma de los numeros: ${list} es ${suma}`);
console.log(`La suma de los numeros: ${list} es ${suma}`);
